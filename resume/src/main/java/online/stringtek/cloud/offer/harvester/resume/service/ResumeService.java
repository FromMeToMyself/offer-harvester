package online.stringtek.cloud.offer.harvester.resume.service;

import online.stringtek.cloud.offer.harvester.common.pojo.resume.Resume;

public interface ResumeService {
    Resume getById(long id);
}
